# 智档使用指南

更新时间：2026-05-26

## 先说怎么用

1. 先登录。
2. 在左侧栏先选中一个客户。
3. 再进入对应页面处理数据。

很多功能都依赖“当前客户”。如果没先选客户，页面会提示你先选客户。

## 页面总览

### `/chat` 对话

用途：用自然语言维护客户预期、场景、客户信息。

怎么用：
1. 在左侧先选客户。
2. 在输入框里直接说需求。
3. 支持查询、修改、新增、删除类指令。
4. 发送后按助手返回结果处理。

适合：
- 查客户现有信息
- 调整预期/场景
- 让系统帮你做自然语言修改

### `/review` 跟进记录

用途：生成和审核跟进记录，最后提交到简道云。

怎么用：
1. 先选客户。
2. 上传会议转写、图片，或者直接粘贴文本。
3. 可从已有转写/跟进记录里勾选若干条，拼接到当前内容。
4. 点击“生成跟进记录”。
5. 检查并编辑正文、跟进类型、日期、关联预期、联系人、出差等字段。
6. 确认无误后提交。

注意：
- 当前客户必须先选好。
- 只有“跟进记录正文”支持客制化模板。
- 预期、联系人、出差仍按固定流程处理。

### `/transcripts` 转写管理

用途：上传会议转写或文件，生成预期场景和跟进相关内容。

怎么用：
1. 先选客户。
2. 选择“会议转写”或“跟进记录”模式。
3. 上传文件，或直接贴文本。
4. 点击分析。
5. 审核 Agent 生成的卡片。
6. 需要的话手动改内容，再提交。

适合：
- 会议转写分析
- 跟进记录/预期场景整理
- 批量审核 create / update / skip 卡片

### `/power-map` 权利地图

用途：查看和维护客户内部关系图、组织图，以及沙箱预览。

怎么用：
1. 先选客户。
2. 在“原版 / 预览 / 树状”之间切换。
3. 原版用于看 BI iframe。
4. 预览用于看沙箱效果。
5. 右侧维护面板可做局部调整、重排和查看修改结果。

注意：
- 预览区看的是服务器上的权利地图 sandbox，不是别的外部链接。

### `/power-map-chat`

用途：用自然语言直接调整权利地图。

怎么用：
1. 先选客户。
2. 在输入框里描述修改目标。
3. 例如新增节点、移动部门、调整层级、改关系。
4. 系统会结合图谱状态和工具调用逐步执行。

## 常见约束

- 先选客户，再做后续操作。
- 跟进记录和转写分析都依赖当前客户。
- 不要在生产环境里乱提交测试数据到简道云。
- 权利地图的预览和原版是两个不同视图，别混了。

## 给新同事的最短路径

1. 登录。
2. 选客户。
3. 先去 `/chat` 熟悉客户状态。
4. 去 `/review` 生成跟进记录。
5. 去 `/transcripts` 处理转写和场景。
6. 需要看客户关系图就去 `/power-map`。
